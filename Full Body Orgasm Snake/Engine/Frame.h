#pragma once
#include "Graphics.h"
class Frame
{
public:
	// Frame();
	void TriLTL(Graphics& gfx); // top left side triangle Left (touching the left side)
private:
	static constexpr float Gap = 20.0f; // lit.: space from left screen to right of the border 
	static constexpr float Width = 20.0f;
	static constexpr float TriWidth = 10.0f;
	// Graphics &gfx; wouldn't have worked here
	// wouldn't have worked here
	// wouldn't have worked here
	// wouldn't have worked here
	// wouldn't have worked here
};

